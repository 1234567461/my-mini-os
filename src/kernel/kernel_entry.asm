; ==========================================
; 内核入口 - kernel_entry.asm
; 功能：从汇编跳转到C语言的kernel_main函数
; ==========================================

[bits 32]
[extern kernel_main]    ; 声明外部函数（在C代码中定义）

call kernel_main        ; 调用C语言内核主函数

jmp $                   ; 如果内核返回，死循环
